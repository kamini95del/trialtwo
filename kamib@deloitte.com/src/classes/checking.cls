//change done for test in branch1
public class checking {
    public void asd(){
        Lead l=new Lead();
   // checking(){}
  Map<id, Lead> lo = new Map<id, Lead>();
   /* for(lead l : [select id, name from lead limit 2]){
        lo.put(l.id, l);
    }*/
    set<id> Leadids = new set<id >();
   // Leadids.addAll(lo.keySet());
    //system.debug('22222222'+Leadids);
    }
}